extends ../layout

block content
<<<<<<< HEAD
  .row.justify-content-center
    .col-md-7.col-lg-6
      .card.shadow-sm.my-5
        .card-header
          h1.h4.mb-0 Editar Usuario: #{usuario.nombre_usuario}
        form(action="/usuarios/actualizar/" + usuario.id_usuario, method="POST")
          .card-body.p-4
            .mb-3
              label.form-label(for="nombre_usuario") Nombre de Usuario
              input.form-control#nombre_usuario(type="text", name="nombre_usuario", value=usuario.nombre_usuario, required)
            .mb-3
              label.form-label(for="email") Email
              input.form-control#email(type="email", name="email", value=usuario.email, required)
            .mb-3
              label.form-label(for="password") Nueva Contraseña (Dejar vacío para no cambiar)
              input.form-control#password(type="password", name="password", placeholder="••••••••")
            .mb-3
              label.form-label(for="rol") Rol
              select.form-select#rol(name="rol", required)
                option(value="admin", selected=(usuario.rol === 'admin')) Administrador
                option(value="medico", selected=(usuario.rol === 'medico')) Médico
                option(value="enfermero", selected=(usuario.rol === 'enfermero')) Enfermero
                option(value="recepcion", selected=(usuario.rol === 'recepcion')) Recepción
          .card-footer.text-end.bg-light
            a.btn.btn-secondary(href="/usuarios") Cancelar
            button.btn.btn-primary.ms-2(type="submit") Actualizar Usuario
=======
  .max-w-md.mx-auto.bg-white.mt-10.p-8.rounded-2xl.shadow-xl
    h1.text-3xl.font-bold.text-blue-900.mb-6 Editar Usuario: #{usuario.nombre_usuario}

    form(action=`/usuarios/actualizar/${usuario.id_usuario}`, method="POST", class="space-y-6")
      div
        label(for="nombre_usuario", class="block text-sm font-medium text-gray-700") Nombre de Usuario
        input(type="text", name="nombre_usuario", id="nombre_usuario", value=usuario.nombre_usuario, required, class="w-full mt-1 rounded-md border-gray-300 shadow-sm focus:ring-blue-500 focus:border-blue-500")
      div
        label(for="email", class="block text-sm font-medium text-gray-700") Email
        input(type="email", name="email", id="email", value=usuario.email, required, class="w-full mt-1 rounded-md border-gray-300 shadow-sm focus:ring-blue-500 focus:border-blue-500")
      div
        label(for="password", class="block text-sm font-medium text-gray-700") Nueva Contraseña (Dejar vacío para no cambiar)
        input(type="password", name="password", id="password", placeholder="********", class="w-full mt-1 rounded-md border-gray-300 shadow-sm focus:ring-blue-500 focus:border-blue-500")
      div
        label(for="rol", class="block text-sm font-medium text-gray-700") Rol
        select(name="rol", id="rol", required, class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:ring-blue-500 focus:border-blue-500")
          option(value="admin", selected=(usuario.rol === 'admin')) Administrador
          option(value="medico", selected=(usuario.rol === 'medico')) Médico
          option(value="enfermero", selected=(usuario.rol === 'enfermero')) Enfermero
          option(value="recepcion", selected=(usuario.rol === 'recepcion')) Recepción

      .flex.justify-end.space-x-4.pt-4
        a(href="/usuarios", class="bg-gray-600 hover:bg-gray-700 transition-colors duration-300 text-white font-semibold py-2 px-4 rounded-md shadow-md") Cancelar
        button(type="submit", class="bg-blue-600 hover:bg-blue-700 transition-colors duration-300 text-white font-semibold py-2 px-6 rounded-md shadow-md") Actualizar Usuario
>>>>>>> aa8027501b4c073def9c20a08ba1531f326b1aa0
