extends ../layout

block content
<<<<<<< HEAD
  .row.justify-content-center
    .col-md-7.col-lg-6
      .card.shadow-sm.my-5
        .card-header
          h1.h4.mb-0 Editar Turno ##{turno.id_turno}
        form(action="/turnos/actualizar/" + turno.id_turno, method="POST")
          .card-body.p-4
            .mb-3
              label.form-label(for="id_paciente") Paciente
              select.form-select#id_paciente(name="id_paciente", required)
                each p in pacientes
                  option(value=p.id_paciente, selected=(p.id_paciente === turno.id_paciente))= p.nombre + ' ' + p.apellido + ' (DNI: ' + p.dni + ')'
            .mb-3
              label.form-label(for="id_medico") Médico
              select.form-select#id_medico(name="id_medico", required)
                each m in medicos
                  option(value=m.id_medico, selected=(m.id_medico === turno.id_medico))= m.nombre + ' ' + m.apellido
            .mb-3
              label.form-label(for="fecha_hora") Fecha y Hora
              input.form-control#fecha_hora(type="datetime-local", name="fecha_hora", value=turno.fecha_hora ? new Date(turno.fecha_hora).toISOString().slice(0, 16) : '', required)
            .mb-3
              label.form-label(for="estado") Estado
              select.form-select#estado(name="estado", required)
                option(value="pendiente", selected=(turno.estado === 'pendiente')) Pendiente
                option(value="confirmado", selected=(turno.estado === 'confirmado')) Confirmado
                option(value="cancelado", selected=(turno.estado === 'cancelado')) Cancelado
                option(value="finalizado", selected=(turno.estado === 'finalizado')) Finalizado
          .card-footer.text-end.bg-light
            a.btn.btn-secondary(href="/turnos") Cancelar
            button.btn.btn-primary.ms-2(type="submit") Actualizar Turno
=======
  .max-w-md.mx-auto.bg-white.mt-10.p-8.rounded-2xl.shadow-xl
    h1.text-3xl.font-bold.text-blue-900.mb-6 Editar Turno ##{turno.id_turno}

    form(action=`/turnos/actualizar/${turno.id_turno}`, method="POST", class="space-y-6")
      div
        label(for="id_paciente", class="block text-sm font-medium text-gray-700") Paciente
        select(name="id_paciente", id="id_paciente", required, class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:ring-blue-500 focus:border-blue-500")
          each p in pacientes
            option(value=p.id_paciente, selected=(p.id_paciente === turno.id_paciente))= p.nombre + ' ' + p.apellido + ' (DNI: ' + p.dni + ')'
      div
        label(for="id_medico", class="block text-sm font-medium text-gray-700") Médico
        select(name="id_medico", id="id_medico", required, class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:ring-blue-500 focus:border-blue-500")
          each m in medicos
            option(value=m.id_medico, selected=(m.id_medico === turno.id_medico))= m.nombre + ' ' + m.apellido
      div
        label(for="fecha_hora", class="block text-sm font-medium text-gray-700") Fecha y Hora
        input(type="datetime-local", name="fecha_hora", id="fecha_hora", value=turno.fecha_hora ? new Date(turno.fecha_hora).toISOString().slice(0, 16) : '', required, class="w-full mt-1 rounded-md border-gray-300 shadow-sm focus:ring-blue-500 focus:border-blue-500")
      div
        label(for="estado", class="block text-sm font-medium text-gray-700") Estado
        select(name="estado", id="estado", required, class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:ring-blue-500 focus:border-blue-500")
          option(value="pendiente", selected=(turno.estado === 'pendiente')) Pendiente
          option(value="confirmado", selected=(turno.estado === 'confirmado')) Confirmado
          option(value="cancelado", selected=(turno.estado === 'cancelado')) Cancelado
          option(value="finalizado", selected=(turno.estado === 'finalizado')) Finalizado

      .flex.justify-end.space-x-4.pt-4
        a(href="/turnos", class="bg-gray-600 hover:bg-gray-700 transition-colors duration-300 text-white font-semibold py-2 px-4 rounded-md shadow-md") Cancelar
        button(type="submit", class="bg-blue-600 hover:bg-blue-700 transition-colors duration-300 text-white font-semibold py-2 px-6 rounded-md shadow-md") Actualizar Turno
>>>>>>> aa8027501b4c073def9c20a08ba1531f326b1aa0
