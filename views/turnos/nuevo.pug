extends ../layout

block content
<<<<<<< HEAD
  .row.justify-content-center
    .col-md-7.col-lg-6
      .card.shadow-sm.my-5
        .card-header
          h1.h4.mb-0 Agendar Nuevo Turno
        form(action="/turnos", method="POST")
          .card-body.p-4
            .mb-3
              label.form-label(for="id_paciente") Paciente
              select.form-select#id_paciente(name="id_paciente", required)
                option(value="") Seleccionar Paciente
                each p in pacientes
                  option(value=p.id_paciente)= p.nombre + ' ' + p.apellido + ' (DNI: ' + p.dni + ')'
            .mb-3
              label.form-label(for="id_medico") Médico
              select.form-select#id_medico(name="id_medico", required)
                option(value="") Seleccionar Médico
                each m in medicos
                  option(value=m.id_medico)= m.nombre + ' ' + m.apellido
            .mb-3
              label.form-label(for="fecha_hora") Fecha y Hora
              input.form-control#fecha_hora(type="datetime-local", name="fecha_hora", required)
            .mb-3
              label.form-label(for="estado") Estado
              select.form-select#estado(name="estado", required)
                option(value="pendiente") Pendiente
                option(value="confirmado") Confirmado
                option(value="cancelado") Cancelado
                option(value="finalizado") Finalizado
          .card-footer.text-end.bg-light
            a.btn.btn-secondary(href="/turnos") Cancelar
            button.btn.btn-primary.ms-2(type="submit") Guardar Turno
=======
  .max-w-md.mx-auto.bg-white.mt-10.p-8.rounded-2xl.shadow-xl
    h1.text-3xl.font-bold.text-blue-900.mb-6 Agendar Nuevo Turno

    form(action="/turnos", method="POST", class="space-y-6")
      div
        label(for="id_paciente", class="block text-sm font-medium text-gray-700") Paciente
        select(name="id_paciente", id="id_paciente", required, class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:ring-blue-500 focus:border-blue-500")
          option(value="") Seleccionar Paciente
          each p in pacientes
            option(value=p.id_paciente)= p.nombre + ' ' + p.apellido + ' (DNI: ' + p.dni + ')'
      div
        label(for="id_medico", class="block text-sm font-medium text-gray-700") Médico
        select(name="id_medico", id="id_medico", required, class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:ring-blue-500 focus:border-blue-500")
          option(value="") Seleccionar Médico
          each m in medicos
            option(value=m.id_medico)= m.nombre + ' ' + m.apellido
      div
        label(for="fecha_hora", class="block text-sm font-medium text-gray-700") Fecha y Hora
        input(type="datetime-local", name="fecha_hora", id="fecha_hora", required, class="w-full mt-1 rounded-md border-gray-300 shadow-sm focus:ring-blue-500 focus:border-blue-500")
      div
        label(for="estado", class="block text-sm font-medium text-gray-700") Estado
        select(name="estado", id="estado", required, class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:ring-blue-500 focus:border-blue-500")
          option(value="pendiente") Pendiente
          option(value="confirmado") Confirmado
          option(value="cancelado") Cancelado
          option(value="finalizado") Finalizado

      .flex.justify-end.space-x-4.pt-4
        a(href="/turnos", class="bg-gray-600 hover:bg-gray-700 transition-colors duration-300 text-white font-semibold py-2 px-4 rounded-md shadow-md") Cancelar
        button(type="submit", class="bg-blue-600 hover:bg-blue-700 transition-colors duration-300 text-white font-semibold py-2 px-6 rounded-md shadow-md") Guardar Turno
>>>>>>> aa8027501b4c073def9c20a08ba1531f326b1aa0
